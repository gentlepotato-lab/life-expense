-- ═══════════════════════════════════════════════════════════════
-- 결제 수단에 "구분" 추가 — 카드 / 계좌·현금 / 간편결제 / 기타
-- 2026-08-17
-- ─────────────────────────────────────────────────────────────
-- Categories 는 중분류가 이미 묶음 역할을 하지만 Payment Methods 는
-- 평평한 목록이라 묶을 기준이 없었다. Counterparts 와 같은 모양으로
-- 구분을 붙인다.
--
-- 값을 강제하지 않고(NULL 허용) 기존 행은 그대로 둔다.
-- 구분을 정하기 전까지는 "구분 없음" 묶음에 모여 보인다.
-- ═══════════════════════════════════════════════════════════════

BEGIN;

ALTER TABLE life_expense.payment_methods
    ADD COLUMN IF NOT EXISTS category varchar(20);

COMMIT;

-- ═══════════════════════════════════════════════════════════════
-- 롤백
--   ALTER TABLE life_expense.payment_methods DROP COLUMN IF EXISTS category;
-- ═══════════════════════════════════════════════════════════════
