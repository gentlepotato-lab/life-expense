-- ═══════════════════════════════════════════════════════════════
-- 상대에 "구분" 추가 — 가족 / 친구 / 직장 / 기타
-- 2026-08-14
-- ─────────────────────────────────────────────────────────────
-- 목록이 늘어나면 이름만으로는 구분이 안 되므로 분류를 붙인다.
-- 값을 강제하지 않고(NULL 허용) 기존 행은 그대로 둔다.
-- ═══════════════════════════════════════════════════════════════

BEGIN;

ALTER TABLE life_expense.counterparts
    ADD COLUMN IF NOT EXISTS category varchar(20);

COMMIT;

-- ═══════════════════════════════════════════════════════════════
-- 롤백
--   ALTER TABLE life_expense.counterparts DROP COLUMN IF EXISTS category;
-- ═══════════════════════════════════════════════════════════════
